# 目标
修改Notification服务的消息发送接口（/notifications [post]），支持群发模型数据集推荐的邮件. 
前端调用该接口时，需要传递运营手动筛选的模型和数据集ID，后台再根据用户的标签补充推荐一个模型和一个数据集.

# 要求
- 使用一个新的场景（Scenario），场景名称为“model_dataset_recommend”
- 群发的用户数量很大，需要使用异步处理，避免阻塞接口调用.
- 对比用户的标签和模型/数据集的标签，推荐用户可能感兴趣的模型/数据集.

# 测试

