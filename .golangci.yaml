linters:
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

    - misspell
    - bodyclose

linters-settings:
  staticcheck:
    checks: ["all", "-SA1029"]
  govet:
    disable:
      - composites
